services:
  - type: web
    name: jackflix
    runtime: node
    buildCommand: npm install && npx puppeteer browsers install chrome
    startCommand: node server.js
    plan: free
    healthCheckPath: /
